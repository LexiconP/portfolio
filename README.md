# portfolio
public portoflio

## Architecture
See [budgetapp/docs/architecture.md](budgetapp/docs/architecture.md).

## Docker
Build and run the API from the app folder:
- `cd budgetapp`
- `docker compose up --build`

Health check:
- `http://localhost:8000/health`

Web UI:
- `http://localhost:8000/`
